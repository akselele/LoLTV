const { leaguepedia } = require('poro');

export default class matchService {
  // async getUpcomingMatches() {
  //   const matchSchedule = await leaguepedia.fetchMatchScheduleGame({
  //     where: ['EU LCS'],
  //   });

  //   return matchSchedule;
  // }

  // async getGames() {
  //   const matchSchedule = await leaguepedia.fetchMatchScheduleGame({
  //     where: [2021, 'LEC', 'Spring'],
  //   });

  //   return matchSchedule;
  // }
}
